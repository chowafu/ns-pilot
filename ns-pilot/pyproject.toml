[project]
name = "ns-pilot"
version = "0.1.0"
description = ""
authors = [
    {name = "Andre",email = "73844275+chowafu@users.noreply.github.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]

[tool.poetry]
packages = [{include = "ns_pilot", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
